// Vercel Web Analytics initialization
// This module imports and initializes Vercel Web Analytics using the inject() function
import { inject } from '@vercel/analytics';

// Initialize Vercel Web Analytics
// inject() runs on the client side and automatically tracks web vitals and page views
inject();
